Read `docs/superpowers/plans/2026-03-28-claude-hooks-automation.md` and execute **Tasks 1-6 and Task 9** in order. Skip Tasks 7 and 8 (Claude handles those skill files).

Summary of what to do:
1. Task 1: Create `scripts/hooks/` directory + add `.claude/.stop-count` to `.gitignore`
2. Task 2: Write `scripts/hooks/file-boundary-guard.sh` with the exact code from the plan, make it executable, run all verification tests
3. Task 3: Write `scripts/hooks/post-edit-check.sh` with the exact code from the plan, make it executable, run verification tests
4. Task 4: Write `scripts/hooks/stop-counter.sh` with the exact code from the plan, make it executable, run verification tests
5. Task 5: Write `scripts/hooks/pre-compact-check.sh` with the exact code from the plan, make it executable, run verification tests
6. Task 6: Write `.claude/settings.json` with the exact JSON from the plan, verify it's valid
9. Task 9: Run integration verification steps from the plan, then commit all changes and push to master

Important:
- All code is pre-written in the plan. Copy it exactly, do not modify.
- Run every verification step documented in the plan for each task.
- After all tasks pass, commit everything and `git push origin master`.
- Use `coding-standards` and `verification-before-completion` skills.
