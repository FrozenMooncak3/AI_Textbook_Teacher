**[DISPATCH TO: Codex]**

## Context
Gemini Flash produces more verbose JSON than Claude. Two endpoints are failing because `maxOutputTokens` is too low — the model output gets truncated mid-JSON, causing parse errors. Logs confirm: guide (1024 tokens) and generate-questions (4096 tokens) both fail on a module with 24 KPs.

## Task
Increase `maxOutputTokens` in two route files:

1. `src/app/api/modules/[moduleId]/guide/route.ts` line 100: change `maxOutputTokens: 1024` → `maxOutputTokens: 4096`
2. `src/app/api/modules/[moduleId]/generate-questions/route.ts` line 167: change `maxOutputTokens: 4096` → `maxOutputTokens: 16384`

That's it. Two numbers, two files.

## Files
- **Modify**: `src/app/api/modules/[moduleId]/guide/route.ts:100`
- **Modify**: `src/app/api/modules/[moduleId]/generate-questions/route.ts:167`

## Acceptance Criteria
- [ ] guide route maxOutputTokens is 4096
- [ ] generate-questions route maxOutputTokens is 16384
- [ ] No other changes
- [ ] Commit and push

## Tier
**Light** — changing two numbers

## Post-Completion
Send completion report via wezterm.
