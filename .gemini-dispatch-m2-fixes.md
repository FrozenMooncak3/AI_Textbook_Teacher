[DISPATCH TO: Gemini] # CCB_CONFIRMED

## Context
M2 Coach AI code review found 4 frontend issues. Backend fixes (C2, I1) already completed by Codex and pushed. Frontend fixes remain.

Plan: `docs/superpowers/plans/2026-03-28-m2-coach-ai.md`
Spec: `docs/superpowers/specs/2026-03-28-m2-coach-ai-design.md`

## Task
Fix 4 code review issues in M2 frontend components.

**Fix 1 (C1 — Critical):** Reading notes duplicate on every save.
In `ModuleLearning.tsx`, `handleSaveNotes` does POST on every blur, creating duplicate `reading_notes` rows. Fix: before POST, call `DELETE /api/modules/{moduleId}/reading-notes` to remove existing notes, then POST current content.

**Fix 2 (I2 — Important):** QA session does not resume on re-entry.
In `QASession.tsx`, `currentIdx` always starts at 0. If user answered some questions and refreshes, they hit 409 ALREADY_ANSWERED. Fix: after loading questions, fetch existing `qa_responses` for each question. Find first question without a response, set `currentIdx` to that index. Pre-populate feedback state for already-answered questions.

**Fix 3 (I3 — Important):** `generate-notes` called from two places.
`QASession.tsx:105` and `NotesDisplay.tsx:34` both call `POST /api/modules/{moduleId}/generate-notes`. Fix: remove the fetch call from `QASession.tsx`. The finalize button should only call `onComplete()`. Let `NotesDisplay` be the sole caller.

**Fix 4 (I4+I5 — Minor):** Remove unused imports in `ModuleLearning.tsx` (useCallback, useRef) and `QASession.tsx` (unused bookId prop).

## Files
- **Modify**: `src/app/books/[bookId]/modules/[moduleId]/ModuleLearning.tsx`
- **Modify**: `src/app/books/[bookId]/modules/[moduleId]/qa/QASession.tsx`
- **Reference**: `src/app/api/modules/[moduleId]/reading-notes/route.ts` (understand DELETE API)
- **Reference**: `src/app/api/modules/[moduleId]/qa-feedback/route.ts` (understand response format)

## Acceptance Criteria
- [ ] Saving reading notes twice does not create duplicate rows
- [ ] Refreshing QA page resumes at first unanswered question
- [ ] `generate-notes` is only called from `NotesDisplay.tsx`
- [ ] No unused imports remain in modified files
- [ ] `npx next build` passes
- [ ] Commit and push to master

## Tier
**Standard** (gemini-2.5-pro)

## Suggested Skills
`coding-standards`, `frontend-patterns`

## Post-Completion
Claude will run requesting-code-review to review changes, then claudemd-check.
