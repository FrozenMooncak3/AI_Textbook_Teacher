---
date: 2026-04-04
topic: M5.5里程碑审计报告通过
type: journal
status: resolved
keywords: [M5.5, 里程碑审计, 页面路由, 组件, 布局]
---

# M5.5 里程碑审计报告

**类型**: audit
**状态**: resolved
**日期**: 2026-04-04

## 审计范围

M5.5 共 15 个 src/ commit，涉及类别：
- 页面路由（布局迁移、错误边界、404）
- 前端组件（侧栏 4 件套、LoadingState）
- API 端点（test/submit error_type 归一化小修）

不涉及：DB schema、AI prompt 模板

## 审计结果

### 页面路由（13 pages + 2 layouts + 3 error + 1 not-found）

全部与 architecture.md 一致。无遗漏、无多余。

### API 端点（34 个）

全部与 architecture.md 一致。M5.5 未新增/删除 API。

### App Shell 契约

| 契约项 | 代码实现 | 结果 |
|--------|---------|------|
| layout.tsx 包裹 SidebarLayout | `<SidebarLayout>{children}</SidebarLayout>` | ✅ |
| SidebarLayout: flex h-screen | `flex h-screen overflow-hidden` | ✅ |
| 内容区: flex-1 overflow-auto | `<main className="flex-1 overflow-auto">` | ✅ |
| 页面用 min-h-full | `<div className="min-h-full">` wraps children | ✅ |
| 三层导航: 全局→书→模块 | L1(首页/上传) L2(阅读/地图/仪表盘/错题) L3(学习/QA/测试/错题) | ✅ |
| localStorage 折叠持久化 | `localStorage.getItem/setItem('sidebar-collapsed')` | ✅ |
| 折叠时 tooltip | `isCollapsed && <div className="...opacity-0 group-hover:opacity-100">` | ✅ |
| 移动端 <1024px 隐藏 | `lg:hidden` hamburger, `-translate-x-full lg:translate-x-0` sidebar | ✅ |
| ESC/backdrop 关闭 | ESC in SidebarProvider, backdrop onClick in Sidebar | ✅ |
| 三级错误边界 + 404 | root/book/module error.tsx + not-found.tsx exist | ✅ |
| LoadingState 双模式 | stage(spinner) + progress(bar) confirmed | ✅ |

### 接口契约

| 契约项 | 代码实现 | 结果 |
|--------|---------|------|
| ProcessingPoller 三阶段 | ocr→extracting→done/error, json.data unwrap | ✅ |
| kp_extraction_status 值 | 'completed'/'failed' (非 done/error) | ✅ |
| 409 边界处理 | ALREADY_COMPLETED + ALREADY_PROCESSING both handled | ✅ |
| test/submit normalizeReviewErrorType | import + 5 处调用点确认 | ✅ |
| MarkdownRenderer 已删除 | 0 references in src/ | ✅ |

### ⚠️ 标记

- 无现存 ⚠️ 标记
- 无新发现需标记的断裂风险

## 结论

architecture.md 已在 commit 1af1d83 中同步更新，与代码完全一致。审计通过。
