# M5.5 用户验收测试反馈

**类型**: bug
**状态**: open
**日期**: 2026-04-04

## 问题 1: test_ch1_2 显示"PDF处理失败"

- 用户点击 test_ch1_2 教材，页面显示"PDF处理失败"
- 但阅读原文页面能看到 PDF 内容
- 疑似 `parse_status` 在 DB 中为 'error'，但 PDF 文件实际存在
- 需要查 DB：`SELECT id, title, parse_status FROM books WHERE title LIKE '%test_ch1%'`

## 问题 2: 读财报模块地图白屏

- 读财报教材的模块地图页面白屏
- 该教材之前是用 TXT 上传测试的（非 PDF）
- 可能是模块地图页面的客户端组件在加载数据时出错，但错误没有被正确捕获
- 需要检查 module-map/page.tsx 的 API 调用和数据格式

## 总体评价

用户确认："其他都没什么问题，都能返回进去，算是完整了。" M5.5 的核心目标（随意导航不报错）基本达成，两个问题是特定数据状态导致的边界情况。
