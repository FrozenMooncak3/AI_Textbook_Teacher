# M4.5 基础设施手动配置完成（T9）

**日期**: 2026-04-22
**执行方式**: Claude 通过 API 自动化完成，用户仅需提供两个 API token
**相关 plan**: `docs/superpowers/plans/2026-04-21-m4.5-pdf-upload-refactor.md` §Task 9
**前置**: T1-T8 代码 + T11 docs 全部 landed（`aafc735…f238c5b`）

## 成果

### 1. Vercel Fluid Compute — ✅ 已是 ON（无需改动）

`GET /v9/projects/prj_T4tJreCNeCpqdf5UXHYtFdfuyA9e` 返回：

```json
"resourceConfig": { "fluid": true, "functionDefaultRegions": ["iad1"] },
"defaultResourceConfig": {
  "fluid": true,
  "functionDefaultRegions": ["iad1"],
  "functionDefaultTimeout": 300,
  "functionDefaultMemoryType": "standard"
}
```

- `fluid: true` — Fluid Compute 已启用
- `functionDefaultTimeout: 300` — fire-and-forget classify / extract-text 的 200s+ 后台任务不会被杀
- Region `iad1` = us-east-1（和 Neon `ep-aged-paper-an78wjk5-pooler.c-6.us-east-1` 同区）

**结论**：不需要 PATCH，不需要 redeploy。

### 2. R2 CORS — ✅ 写入并验证

**CF native API 路径**（`PUT /accounts/{id}/r2/buckets/{bucket}/cors`）成功，**不是** S3-compat `PutBucketCors`（前期已被 R2 access key 权限不足拒）。

**Policy**（`.ccb/r2-cors-cf-native.json`）：
```json
{
  "rules": [{
    "allowed": {
      "origins": ["https://ai-textbook-teacher.vercel.app", "http://localhost:3000"],
      "methods": ["GET", "PUT", "HEAD"],
      "headers": ["Content-Type"]
    },
    "exposeHeaders": ["ETag", "Content-Length", "Content-Type"],
    "maxAgeSeconds": 3600
  }]
}
```

**PUT response**：`{"success":true,"errors":[],"messages":[],"result":{}}`

**GET 读回**：rules 与 PUT payload 字段级匹配。

**OPTIONS preflight 验证**（`curl -X OPTIONS ... -H "Origin: https://ai-textbook-teacher.vercel.app"`）：
```
HTTP/1.1 204 No Content
Access-Control-Allow-Origin: https://ai-textbook-teacher.vercel.app
Vary: Origin
Access-Control-Allow-Headers: Content-Type
Access-Control-Allow-Methods: GET, PUT, HEAD
Access-Control-Max-Age: 3600
```

## 操作细节

- **Token 1**: Vercel 30-day token（vercel.com → Account Settings → Tokens），存 `reference_vercel-deployment.md` memory
- **Token 2**: Cloudflare Custom Token，"Workers R2 Storage: Edit" 权限（CF 控制台把 R2 归在 Workers 分类下），TTL 到 2026-05-31，同存 memory

## 复验命令（未来如需自查）

```bash
# Vercel Fluid 状态
curl -sS -H "Authorization: Bearer $VERCEL_TOKEN" \
  "https://api.vercel.com/v9/projects/prj_T4tJreCNeCpqdf5UXHYtFdfuyA9e" \
  | jq '.resourceConfig, .defaultResourceConfig'

# R2 CORS 规则
curl -sS -H "Authorization: Bearer $CF_TOKEN" \
  "https://api.cloudflare.com/client/v4/accounts/65d209c1e56b05aea00d9db429a61910/r2/buckets/ai-textbook-pdfs/cors"

# OPTIONS 预检
curl -sSi -X OPTIONS "https://65d209c1e56b05aea00d9db429a61910.r2.cloudflarestorage.com/ai-textbook-pdfs/books/1/original.pdf" \
  -H "Origin: https://ai-textbook-teacher.vercel.app" \
  -H "Access-Control-Request-Method: PUT" \
  -H "Access-Control-Request-Headers: Content-Type"
```

## 下一步

T10 — 14.2MB 真书生产端到端压测。T1-T9 全部就位，可以直接上传验证。
