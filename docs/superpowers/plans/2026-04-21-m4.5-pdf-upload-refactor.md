# M4.5 PDF 上传重构 + 准备页 UX 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `task-execution` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 presigned URL 直传 R2 + fire-and-forget 处理 + 准备页 polling，让 14.2MB / 369 页中文教材端到端跑通，满足 "perceived 5s 进准备页 + perceived 30s 第一模块就绪" 硬验收。

**Architecture:**
- 上传：浏览器调 `/api/uploads/presign` 拿 PUT URL → XHR 直传 R2 → 调 `/api/books/confirm` → Next.js fire-and-forget 启动 classify + extract，1s 内返回
- 准备页：Next.js App Router `loading.tsx` 骨架屏秒开 + 客户端 2s polling `/api/books/[bookId]/status` → 第一模块 kp_extraction_status='completed' 时解锁按钮 → 用户手动跳 reader
- DB：books 加 `upload_status` + `file_size` 2 列，幂等迁移；现有 books 默认 `upload_status='confirmed'` 不受影响

**Tech Stack:**
- Next.js 15 App Router (Server Components + Client polling)
- PostgreSQL (Neon) + `@aws-sdk/s3-request-presigner`（唯一新增依赖）
- Cloudflare R2（S3-兼容，现有 `buildObjectKey(bookId) = books/<bookId>/original.pdf` 约定沿用）
- XMLHttpRequest `upload.onprogress`（fetch 无原生进度事件）
- Vercel Fluid Compute（300s 函数上限）+ Sentry（已接）

**Spec 来源**：`docs/superpowers/specs/2026-04-21-pdf-upload-refactor-design.md`

**CCB 分工**（task-execution 按此派发）：
- 后端（T1-T6 + T11 docs）→ Codex · `AGENTS.md`
- 前端（T7-T8）→ Gemini · `GEMINI.md`
- 基础设施手动（T9）+ 压测（T10）→ 用户 + Claude 监控

---

## File Structure

### 新增文件（5 个）

| 路径 | 责任 |
|---|---|
| `src/app/api/uploads/presign/route.ts` | Presigned URL 签发端点：校验 → INSERT books → 返回 PUT URL |
| `src/app/api/books/confirm/route.ts` | Confirm 端点：HEAD R2 校验 → UPDATE books → fire-and-forget classify+extract |
| `src/app/books/[bookId]/preparing/page.tsx` | 准备页主组件：2s polling + 骨架屏 + 第一模块就绪解锁按钮 |
| `src/app/books/[bookId]/preparing/loading.tsx` | Next.js App Router 初始骨架屏（navigation 瞬间 0ms 显示） |
| `src/lib/upload-flow.ts` | fire-and-forget 处理链抽取（保持 route.ts 瘦身） |

### 调整文件（4 个）

| 路径 | 调整点 |
|---|---|
| `src/lib/schema.sql` | 追加 2 条 `ALTER TABLE books ADD COLUMN IF NOT EXISTS` 幂等 DDL |
| `src/lib/r2-client.ts` | 新增 `buildPresignedPutUrl(bookId, expirySeconds)` 函数 |
| `src/app/api/books/route.ts` | 删除 PDF 分支（返 400 引导），保留 TXT 分支 |
| `src/app/api/books/[bookId]/status/route.ts` | 在现有响应上追加 7 个新字段，保留所有旧字段（ProcessingPoller 依赖） |
| `src/app/upload/page.tsx` | 重写"按按钮后"逻辑，视觉骨架不动，加 PDF/TXT 分流 + 6 态状态机 + XHR progress |

### 不改文件（确认）

- `src/app/api/books/[bookId]/pdf/route.ts` — 继续用 `buildObjectKey(bookId)` 读对象，object key 约定不变
- `src/app/api/ocr/callback/route.ts` — 扫描 PDF 回调链路不动
- `src/lib/services/kp-extraction-service.ts` — `triggerReadyModulesExtraction` 沿用
- `src/app/books/[bookId]/reader/page.tsx` — reader 页不动

---

## Task 1: DB Schema — 追加 upload_status + file_size 列

**Files:**
- Modify: `src/lib/schema.sql` (L11-25 区域后追加 2 行)

**Context:** books 表当前无上传阶段状态列。M4.5 引入两阶段上传（presign → confirm），需要 `upload_status` 标识是否已确认。`file_size` 用于 confirm 端点 HEAD 校验上传完整性。

- [ ] **Step 1: 读 schema.sql 确认 books 表定义位置**

Run: Read `src/lib/schema.sql`, note the books table (L11-25) and the existing `ALTER TABLE books ADD COLUMN IF NOT EXISTS` pattern (L27-28).

- [ ] **Step 2: 追加 2 条幂等 ALTER TABLE 到 books 表外（与 learning_mode 同区域）**

紧接 `ALTER TABLE books ADD COLUMN IF NOT EXISTS preferred_learning_mode TEXT;` 后插入：

```sql
-- M4.5: upload stage tracking (for presigned URL upload flow)
ALTER TABLE books ADD COLUMN IF NOT EXISTS upload_status TEXT NOT NULL DEFAULT 'confirmed';
ALTER TABLE books ADD COLUMN IF NOT EXISTS file_size BIGINT NOT NULL DEFAULT 0;

-- Ensure CHECK constraint is present (idempotent via DROP IF EXISTS)
DO $$
BEGIN
  IF NOT EXISTS (
    SELECT 1 FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    WHERE t.relname = 'books' AND c.conname = 'books_upload_status_check'
  ) THEN
    ALTER TABLE books ADD CONSTRAINT books_upload_status_check
      CHECK (upload_status IN ('pending', 'confirmed'));
  END IF;
END $$;
```

**Rationale:** `DEFAULT 'confirmed'` 让现有 books 自动回填为 'confirmed'（它们已处理完）；新上传书由 `/presign` 显式 INSERT `'pending'` 覆写默认值。TXT 分支继续不传 upload_status，落默认 'confirmed' 正确（TXT 立刻完成）。

**与 spec §1.2 的偏差说明：** spec 原描述 5 步迁移序列（ADD DEFAULT NULL → UPDATE 回填 → ALTER NOT NULL → SET DEFAULT 'pending' → ADD CHECK）。本 plan 简化为单步 `DEFAULT 'confirmed'`——原因：INSERT 路径（`/presign` Task 3）显式写 `'pending'` 覆写默认值，不会被 'confirmed' 误伤；`IF NOT EXISTS` 保证幂等 re-run 安全。结果等价但更简洁。

- [ ] **Step 3: 本地启动 dev 服务器触发 initDb**

Run: `npm run dev`
Expected: 启动日志无错误，DB migration 执行成功。

- [ ] **Step 4: 本地用 psql 验证列已加**

Run (用户执行):
```
psql $DATABASE_URL -c "\d books"
```
Expected: `upload_status | text` + `file_size | bigint` 两列可见，upload_status 有 CHECK 约束。

- [ ] **Step 5: 验证现有数据回填正确**

Run:
```
psql $DATABASE_URL -c "SELECT id, title, upload_status, file_size FROM books ORDER BY id LIMIT 5;"
```
Expected: 所有现有行 `upload_status='confirmed'`，`file_size=0`。

- [ ] **Step 6: Commit**

```bash
git add src/lib/schema.sql
git commit -m "feat(m4.5): add upload_status + file_size columns to books

Two new columns for two-phase upload (presign → confirm) flow.
Existing books auto-backfill to 'confirmed' via DEFAULT; new /presign
inserts will explicitly write 'pending'. TXT branch unaffected.

Refs: spec §1.1, §7.3"
```

---

## Task 2: R2 Client — 新增 buildPresignedPutUrl

**Files:**
- Modify: `src/lib/r2-client.ts` (追加 1 函数)
- Dependency: `@aws-sdk/s3-request-presigner`（确认已装 — 现有 L2 import 说明已在依赖树中）

**Context:** 现有 `getSignedPdfUrl` 签 GET URL，now 需要 PUT URL。SDK 已有，只差一个 wrapper。

- [ ] **Step 1: 确认 `@aws-sdk/s3-request-presigner` 已在 package.json**

Run: `grep 's3-request-presigner' package.json`
Expected: 若存在 → 跳 Step 2；若无 → `npm install @aws-sdk/s3-request-presigner`

- [ ] **Step 2: 在 r2-client.ts 追加 `buildPresignedPutUrl` 函数**

在 `getSignedPdfUrl` 下方（L70 后）插入：

```ts
export async function buildPresignedPutUrl(
  bookId: number,
  expirySeconds: number = 900
): Promise<{ uploadUrl: string; objectKey: string }> {
  const { bucket } = readConfig()
  const objectKey = buildObjectKey(bookId)

  const uploadUrl = await getSignedUrl(
    getR2Client(),
    new PutObjectCommand({
      Bucket: bucket,
      Key: objectKey,
      ContentType: 'application/pdf',
    }),
    { expiresIn: expirySeconds }
  )

  return { uploadUrl, objectKey }
}
```

**Notes:**
- 沿用 `buildObjectKey(bookId)` 获 key，保持 `books/<bookId>/original.pdf` 约定
- `expirySeconds` 默认 900（15 分钟，覆盖极慢网 14.2MB 上传）
- 显式传 `ContentType: 'application/pdf'` 与浏览器 PUT header 一致，R2 会校验

- [ ] **Step 3: 添加单元测试到 `src/lib/r2-client.test.ts`**

在文件末尾追加：

```ts
test('buildPresignedPutUrl returns URL and object key for valid bookId', async () => {
  process.env.R2_ACCOUNT_ID = 'test-account'
  process.env.R2_ACCESS_KEY_ID = 'test-key'
  process.env.R2_SECRET_ACCESS_KEY = 'test-secret'
  process.env.R2_BUCKET = 'test-bucket'

  const mod = await import('./r2-client')
  const { uploadUrl, objectKey } = await (mod as {
    buildPresignedPutUrl: (id: number) => Promise<{ uploadUrl: string; objectKey: string }>
  }).buildPresignedPutUrl(42)

  expect(objectKey).toBe('books/42/original.pdf')
  expect(uploadUrl).toMatch(/^https?:\/\/.*\.r2\.cloudflarestorage\.com/)
  expect(uploadUrl).toContain('X-Amz-Signature')
  expect(uploadUrl).toContain('X-Amz-Expires=900')
})
```

- [ ] **Step 4: 运行测试**

Run: `npm test -- src/lib/r2-client.test.ts`
Expected: PASS（包括 `buildObjectKey` 旧测试 + 新 `buildPresignedPutUrl` 测试）

- [ ] **Step 5: Commit**

```bash
git add src/lib/r2-client.ts src/lib/r2-client.test.ts package.json package-lock.json
git commit -m "feat(m4.5): add buildPresignedPutUrl for presigned PUT URLs

Wraps @aws-sdk/s3-request-presigner PutObjectCommand signer.
Reuses existing buildObjectKey convention (books/<bookId>/original.pdf)
to stay compatible with GET /api/books/[bookId]/pdf endpoint.

TTL 900s (15min) covers very slow networks uploading 14.2MB files.

Refs: spec §2.1"
```

---

## Task 3: POST /api/uploads/presign 端点

**Files:**
- Create: `src/app/api/uploads/presign/route.ts`

**Context:** 新端点，前端上传入口。校验 session + 文件大小 + 类型，INSERT book 行（upload_status='pending'），调 `buildPresignedPutUrl` 返回 PUT URL。

- [ ] **Step 1: 创建路由文件，写完整实现**

Create: `src/app/api/uploads/presign/route.ts`

```ts
import { z } from 'zod'
import { requireUser } from '@/lib/auth'
import { handleRoute } from '@/lib/handle-route'
import { insert } from '@/lib/db'
import { logAction } from '@/lib/log'
import { buildPresignedPutUrl } from '@/lib/r2-client'
import { UserError } from '@/lib/errors'

const RequestSchema = z.object({
  filename: z.string().min(1).max(255),
  size: z.number().int().positive().max(50 * 1024 * 1024),
  contentType: z.literal('application/pdf'),
})

export const POST = handleRoute(async (req) => {
  const user = await requireUser(req)
  const userId = user.id

  const body = await req.json().catch(() => null)
  const parsed = RequestSchema.safeParse(body)
  if (!parsed.success) {
    throw new UserError(
      parsed.error.issues[0]?.message ?? 'Invalid request',
      'INVALID_REQUEST',
      400
    )
  }

  const { filename, size } = parsed.data

  const bookId = await insert(
    `INSERT INTO books (user_id, title, raw_text, parse_status, kp_extraction_status, upload_status, file_size)
     VALUES ($1, $2, '', 'pending', 'pending', 'pending', $3)
     RETURNING id`,
    [userId, filename, size]
  )

  const { uploadUrl, objectKey } = await buildPresignedPutUrl(bookId, 900)

  await logAction(
    'book_presign_issued',
    `bookId=${bookId}, filename=${filename}, size=${size}`
  )

  return { data: { bookId, uploadUrl, objectKey } }
})
```

**Notes:**
- `title` 用 filename 占位，confirm 阶段 UPDATE 为用户输入的书名
- `raw_text = ''` 与现有 PDF 分支 L85 一致
- `parse_status` / `kp_extraction_status` / `upload_status` 全显式置 `'pending'`，避免被默认 'confirmed' 坑到
- `file_size` 写入请求 size，供 confirm HEAD 校验

- [ ] **Step 2: 本地启动 dev 服务器**

Run: `npm run dev`

- [ ] **Step 3: curl 测试（未登录 → 401）**

Run:
```bash
curl -X POST http://localhost:3000/api/uploads/presign \
  -H "Content-Type: application/json" \
  -d '{"filename":"test.pdf","size":1000000,"contentType":"application/pdf"}'
```
Expected: HTTP 401 `{"error":"..."}`.

- [ ] **Step 4: curl 测试（已登录、合法请求）**

用户先在浏览器登录，拿 cookie。再 curl：

```bash
curl -X POST http://localhost:3000/api/uploads/presign \
  -H "Content-Type: application/json" \
  -H "Cookie: <session-cookie>" \
  -d '{"filename":"test.pdf","size":1000000,"contentType":"application/pdf"}'
```
Expected: HTTP 200 `{"data":{"bookId": <int>, "uploadUrl":"https://...r2.cloudflarestorage.com/...", "objectKey":"books/<bookId>/original.pdf"}}`。

- [ ] **Step 5: curl 测试（超 50MB → 400）**

```bash
curl -X POST http://localhost:3000/api/uploads/presign \
  -H "Content-Type: application/json" \
  -H "Cookie: <session-cookie>" \
  -d '{"filename":"huge.pdf","size":52428801,"contentType":"application/pdf"}'
```
Expected: HTTP 400，error 文案明确。

- [ ] **Step 6: 验证 DB 行**

```bash
psql $DATABASE_URL -c "SELECT id, title, upload_status, file_size, parse_status FROM books WHERE id = <new-bookId>;"
```
Expected: `upload_status='pending'`, `file_size=1000000`, `parse_status='pending'`。

- [ ] **Step 7: Commit**

```bash
git add src/app/api/uploads/presign/route.ts
git commit -m "feat(m4.5): add POST /api/uploads/presign endpoint

Pre-creates book row with upload_status='pending' and issues R2 PUT
presigned URL with 15min TTL. Frontend immediately gets bookId to
navigate to /preparing page (perceived 5s acceptance).

Size cap: 50MB. ContentType required application/pdf.

Refs: spec §2.1"
```

---

## Task 4: POST /api/books/confirm 端点

**Files:**
- Create: `src/app/api/books/confirm/route.ts`
- Create: `src/lib/upload-flow.ts`（抽取 fire-and-forget 处理链）

**Context:** Confirm 是"关键路径+异步"的混合——同步部分校验 + UPDATE（≤1s 返回），fire-and-forget 部分启 classify + extract 链。

- [ ] **Step 1: 创建 `src/lib/upload-flow.ts` 抽取处理链**

Create: `src/lib/upload-flow.ts`

```ts
import { run } from '@/lib/db'
import { logAction } from '@/lib/log'
import { buildOcrHeaders } from '@/lib/ocr-auth'
import { triggerReadyModulesExtraction } from '@/lib/services/kp-extraction-service'

async function markOcrFailure(bookId: number, details: string) {
  try {
    await run("UPDATE books SET parse_status = 'error' WHERE id = $1", [bookId])
  } catch {
    /* ignore */
  }
  try {
    await logAction('book_ocr_failed', `bookId=${bookId}, ${details}`, 'error')
  } catch {
    /* ignore */
  }
}

export async function runClassifyAndExtract(
  bookId: number,
  r2ObjectKey: string
): Promise<void> {
  const ocrBase = process.env.OCR_SERVER_URL || 'http://127.0.0.1:8000'

  try {
    // Step A: classify-pdf
    const classifyUrl = `${ocrBase}/classify-pdf`
    const classifyRes = await fetch(classifyUrl, {
      method: 'POST',
      headers: await buildOcrHeaders(classifyUrl),
      body: JSON.stringify({ r2_object_key: r2ObjectKey, book_id: bookId }),
    })
    if (!classifyRes.ok) {
      await markOcrFailure(bookId, `classify-pdf HTTP ${classifyRes.status}`)
      return
    }
    const classifyJson = await classifyRes.json() as {
      pages: { page: number; type: string }[]
      text_count: number
      scanned_count: number
      mixed_count: number
      total_pages: number
    }

    const { pages, text_count, scanned_count, mixed_count, total_pages } = classifyJson
    const nonTextPages = scanned_count + mixed_count

    await run(
      `UPDATE books SET
         page_classifications = $1,
         text_pages_count = $2,
         scanned_pages_count = $3,
         ocr_total_pages = $4
       WHERE id = $5`,
      [JSON.stringify(pages), text_count, scanned_count, total_pages, bookId]
    )

    // Step B: extract-text (sync await, creates modules)
    const extractUrl = `${ocrBase}/extract-text`
    const extractRes = await fetch(extractUrl, {
      method: 'POST',
      headers: await buildOcrHeaders(extractUrl),
      body: JSON.stringify({ r2_object_key: r2ObjectKey, book_id: bookId, classifications: pages }),
    })
    if (!extractRes.ok) {
      await markOcrFailure(bookId, `extract-text HTTP ${extractRes.status}`)
      return
    }

    // Step C: if mixed/scanned pages exist, fire-and-forget ocr-pdf
    if (nonTextPages > 0) {
      const ocrPdfUrl = `${ocrBase}/ocr-pdf`
      const ocrPdfHeaders = await buildOcrHeaders(ocrPdfUrl)
      void fetch(ocrPdfUrl, {
        method: 'POST',
        headers: ocrPdfHeaders,
        body: JSON.stringify({ r2_object_key: r2ObjectKey, book_id: bookId, classifications: pages }),
      })
        .then(async (res) => {
          if (!res.ok) {
            const text = await res.text().catch(() => '')
            await markOcrFailure(
              bookId,
              text.trim()
                ? `ocr-pdf HTTP ${res.status}: ${text.trim().slice(0, 500)}`
                : `ocr-pdf HTTP ${res.status}`
            )
          }
        })
        .catch(async (err) => {
          await markOcrFailure(bookId, `ocr-pdf call failed: ${String(err)}`)
        })
    } else {
      await run("UPDATE books SET parse_status = 'done' WHERE id = $1", [bookId])
    }

    // Step D: fire-and-forget KP extraction
    void triggerReadyModulesExtraction(bookId).catch(async (err) => {
      await logAction(
        'triggerReadyModulesExtraction error',
        `bookId=${bookId}: ${String(err)}`,
        'error'
      )
    })

    await logAction(
      'book_upload_classified',
      `bookId=${bookId}, text=${text_count}, scanned=${scanned_count}, mixed=${mixed_count}`
    )
  } catch (error) {
    await markOcrFailure(bookId, `upload flow error: ${String(error)}`)
  }
}
```

**Notes:**
- 这段代码几乎是 `src/app/api/books/route.ts` L91-213 的复制，抽成独立函数让 confirm 路由短
- 唯一改动：去掉 NextResponse.json 返回（现在是 void fire-and-forget）

- [ ] **Step 2: 创建 confirm 路由文件**

Create: `src/app/api/books/confirm/route.ts`

```ts
import { z } from 'zod'
import { HeadObjectCommand } from '@aws-sdk/client-s3'
import { requireUser } from '@/lib/auth'
import { handleRoute } from '@/lib/handle-route'
import { queryOne, run } from '@/lib/db'
import { logAction } from '@/lib/log'
import { buildObjectKey } from '@/lib/r2-client'
import { UserError } from '@/lib/errors'
import { runClassifyAndExtract } from '@/lib/upload-flow'
import { S3Client } from '@aws-sdk/client-s3'

const RequestSchema = z.object({
  bookId: z.number().int().positive(),
  title: z.string().min(1).max(255),
})

interface BookRow {
  id: number
  user_id: number
  upload_status: string
  parse_status: string
  file_size: number
}

// Lazy R2 client for HEAD (mirrors r2-client.ts internal pattern)
let cachedClient: S3Client | null = null
function getR2Client(): S3Client {
  if (cachedClient) return cachedClient
  const accountId = process.env.R2_ACCOUNT_ID!
  const accessKeyId = process.env.R2_ACCESS_KEY_ID!
  const secretAccessKey = process.env.R2_SECRET_ACCESS_KEY!
  cachedClient = new S3Client({
    region: 'auto',
    endpoint: `https://${accountId}.r2.cloudflarestorage.com`,
    forcePathStyle: true,
    credentials: { accessKeyId, secretAccessKey },
  })
  return cachedClient
}

export const POST = handleRoute(async (req) => {
  const user = await requireUser(req)
  const userId = user.id
  const body = await req.json().catch(() => null)
  const parsed = RequestSchema.safeParse(body)
  if (!parsed.success) {
    throw new UserError(parsed.error.issues[0]?.message ?? 'Invalid request', 'INVALID_REQUEST', 400)
  }

  const { bookId, title } = parsed.data

  const book = await queryOne<BookRow>(
    `SELECT id, user_id, upload_status, parse_status, file_size
     FROM books WHERE id = $1 AND user_id = $2`,
    [bookId, userId]
  )
  if (!book) {
    throw new UserError('Book not found', 'NOT_FOUND', 404)
  }

  // Idempotency: if already confirmed + still processing/done, short-circuit OK
  // If confirmed but failed, throw UserError → 409 so frontend can show "delete and retry" UI
  if (book.upload_status === 'confirmed') {
    if (book.parse_status === 'error') {
      await logAction('book_confirm_already_failed', `bookId=${bookId}`, 'error')
      throw new UserError('上一次处理失败，请删除书重新上传', 'PROCESSING_FAILED', 409)
    }
    return { data: { bookId, processing: true } }
  }

  // HEAD R2 to verify object exists + size matches
  const objectKey = buildObjectKey(bookId)
  const bucket = process.env.R2_BUCKET!
  try {
    const headRes = await getR2Client().send(new HeadObjectCommand({ Bucket: bucket, Key: objectKey }))
    const actualSize = headRes.ContentLength ?? 0
    if (book.file_size > 0 && actualSize !== book.file_size) {
      await logAction(
        'book_upload_size_mismatch',
        `bookId=${bookId}, expected=${book.file_size}, actual=${actualSize}`,
        'error'
      )
      throw new UserError('Upload size mismatch', 'UPLOAD_INCOMPLETE', 400)
    }
  } catch (err) {
    if (err instanceof UserError) throw err
    await logAction('book_upload_missing_object', `bookId=${bookId}, err=${String(err)}`, 'error')
    throw new UserError('Upload not found in storage', 'UPLOAD_INCOMPLETE', 400)
  }

  // Atomic UPDATE: mark confirmed + set real title + flip parse_status to processing
  await run(
    `UPDATE books SET upload_status = 'confirmed', parse_status = 'processing', title = $1
     WHERE id = $2 AND upload_status = 'pending'`,
    [title.trim(), bookId]
  )

  // Fire-and-forget the classify+extract chain (returns immediately)
  void runClassifyAndExtract(bookId, objectKey).catch(async (err) => {
    await logAction('runClassifyAndExtract unhandled', `bookId=${bookId}: ${String(err)}`, 'error')
  })

  await logAction('book_confirmed', `bookId=${bookId}, title="${title.trim()}"`)

  return { data: { bookId, processing: true } }
})
```

**Notes:**
- 409 走 `throw new UserError(..., 'PROCESSING_FAILED', 409)` — `handleRoute` 自动产出 `{ success: false, error: '...', code: 'PROCESSING_FAILED' }` status 409，前端按 `confirmJson.code === 'PROCESSING_FAILED'` 识别（Task 7 Step 3）
- HEAD R2 先做，避免假 bookId 绕过校验
- `file_size === 0` 视为老书迁移行，跳过 size match（仅对存在场景做硬校验）

- [ ] **Step 3: 本地 curl 测试（未登录 → 401）**

```bash
curl -X POST http://localhost:3000/api/books/confirm \
  -H "Content-Type: application/json" \
  -d '{"bookId":1,"title":"test"}'
```
Expected: 401

- [ ] **Step 4: 本地 curl 测试（bookId 不归属当前用户 → 404）**

```bash
curl -X POST http://localhost:3000/api/books/confirm \
  -H "Content-Type: application/json" \
  -H "Cookie: <session>" \
  -d '{"bookId":99999,"title":"test"}'
```
Expected: 404

- [ ] **Step 5: 本地 curl 测试（R2 无对象 → 400 UPLOAD_INCOMPLETE）**

先跑 Task 3 拿 bookId + uploadUrl，**不 PUT R2**，直接调 confirm：
```bash
curl -X POST http://localhost:3000/api/books/confirm \
  -H "Cookie: <session>" \
  -d "{\"bookId\":<id>,\"title\":\"测试书\"}"
```
Expected: 400 `UPLOAD_INCOMPLETE`。

- [ ] **Step 6: 本地完整链路测试（presign + PUT + confirm）**

用小 PDF（<4.5MB）测完整链路：

```bash
# 1. presign
curl -X POST http://localhost:3000/api/uploads/presign \
  -H "Cookie: <session>" \
  -d '{"filename":"small.pdf","size":100000,"contentType":"application/pdf"}' \
  -o /tmp/presign.json
BOOKID=$(jq -r .data.bookId /tmp/presign.json)
URL=$(jq -r .data.uploadUrl /tmp/presign.json)

# 2. PUT to R2
curl -X PUT "$URL" -H "Content-Type: application/pdf" --data-binary @small.pdf

# 3. confirm
curl -X POST http://localhost:3000/api/books/confirm \
  -H "Cookie: <session>" \
  -d "{\"bookId\":$BOOKID,\"title\":\"小测试书\"}"
```
Expected: 200 `{"data":{"bookId":<id>,"processing":true}}`，~1s 内返回。

- [ ] **Step 7: 观察 dev 日志确认 fire-and-forget 链路启动**

看 dev 终端：应该能看到 `classify-pdf` fetch、`book_upload_classified` logAction、parse_status 从 'processing' → 'done'（或 'error' + Sentry）。

- [ ] **Step 8: 测试幂等性**

重复 Step 6 的第 3 步 confirm 调用。
Expected: 第二次返回 200 但不触发 classify（日志里没有第二次 classify-pdf fetch）。

- [ ] **Step 9: Commit**

```bash
git add src/app/api/books/confirm/route.ts src/lib/upload-flow.ts
git commit -m "feat(m4.5): add POST /api/books/confirm with fire-and-forget processing

Validates R2 object presence/size via HEAD, atomically flips
upload_status to 'confirmed', then fire-and-forget launches the
classify + extract pipeline. Returns within ~1s so upload page can
redirect to preparing page without blocking on long processing.

Idempotent: confirmed+ok → short-circuit; confirmed+error → 409 with
delete-and-retry hint.

Extracted runClassifyAndExtract to src/lib/upload-flow.ts for reuse.

Refs: spec §2.2"
```

---

## Task 5: 调整 POST /api/books — 删除 PDF 分支

**Files:**
- Modify: `src/app/api/books/route.ts`（删除 L83-214 PDF 分支）

**Context:** 老 PDF 分支违反 4.5MB body 上限，必须 block 用户误用这个端点。TXT 分支保留不动。

- [ ] **Step 1: 读当前 route.ts，确认 TXT vs PDF 分支位置**

Run: Read `src/app/api/books/route.ts`
确认 L67-81 是 TXT 分支，L83-214 是 PDF 分支（含 buildObjectKey/uploadPdf/classify-pdf/extract-text 等）。

- [ ] **Step 2: 用一个 "if PDF → 400" 替换 L83-214**

用以下代码替换 L83-214（整个 PDF 分支，保留 `return` 前的 TXT 分支 L67-81）：

```ts
  if (ext === 'pdf') {
    await logAction('book_upload_pdf_via_old_endpoint', `user=${userId}`, 'error')
    return NextResponse.json(
      {
        error: 'PDF uploads must use /api/uploads/presign',
        code: 'USE_PRESIGN_ENDPOINT',
      },
      { status: 400 }
    )
  }

  return NextResponse.json({ error: 'Unsupported file type' }, { status: 400 })
```

**Notes:**
- `ext` 变量应已在上游 filename 解析中定义（见 L66 之前）
- 若当前代码用 `.endsWith('.txt')` 等分支判断，保持相同判断逻辑，只把 PDF 分支改成 400

- [ ] **Step 3: 删除不再用的 import**

删除 route.ts 顶部：
- `buildObjectKey` / `uploadPdf` from `@/lib/r2-client`
- `buildOcrHeaders` from `@/lib/ocr-auth`
- `triggerReadyModulesExtraction` from `kp-extraction-service`

保留 TXT 分支仍需的 imports（`insert` / `logAction` / `NextResponse` / `requireUser` 等；注意 auth helper 是 `requireUser`，返回 `User` 对象需 `.id`）。

- [ ] **Step 4: 本地 curl 测试（TXT 仍 work）**

```bash
curl -X POST http://localhost:3000/api/books \
  -H "Cookie: <session>" \
  -F "file=@test.txt" \
  -F "title=测试 TXT"
```
Expected: 201 `{"bookId":<id>}`，book 行创建，parse_status='done'，raw_text 有内容。

- [ ] **Step 5: 本地 curl 测试（PDF 走老端点 → 400）**

```bash
curl -X POST http://localhost:3000/api/books \
  -H "Cookie: <session>" \
  -F "file=@small.pdf" \
  -F "title=PDF via old path"
```
Expected: 400，error 文案包含 "USE_PRESIGN_ENDPOINT"。

- [ ] **Step 6: Commit**

```bash
git add src/app/api/books/route.ts
git commit -m "feat(m4.5): remove PDF branch from POST /api/books

All PDF uploads must now use /api/uploads/presign → PUT R2 →
/api/books/confirm. TXT branch retained unchanged.

The old PDF path violated Vercel's 4.5MB body limit for files >4.5MB;
14.2MB test case failed at arrayBuffer() step.

Refs: spec §2.3"
```

---

## Task 6: 扩展 GET /api/books/[bookId]/status

**Files:**
- Modify: `src/app/api/books/[bookId]/status/route.ts`

**Context:** status route 已存在（M1 扫描 PDF 时建），ProcessingPoller / ActionHub 靠它。本任务在现有响应上**追加** 7 个新字段，**保留**所有旧字段。

- [ ] **Step 1: 读当前 status/route.ts 确认结构**

Run: Read `src/app/api/books/[bookId]/status/route.ts`
确认现有 response 有 `parseStatus / ocrCurrentPage / ocrTotalPages + snake_case dual` 且有 `done→completed` / `error→failed` 归一化。

- [ ] **Step 2: 重写 route.ts，追加新字段**

替换整个文件为：

```ts
import { requireBookOwner } from '@/lib/auth'
import { handleRoute } from '@/lib/handle-route'
import { queryOne, query } from '@/lib/db'
import { UserError } from '@/lib/errors'

interface BookStatusRow {
  id: number
  upload_status: string
  parse_status: string
  kp_extraction_status: string
  ocr_current_page: number
  ocr_total_pages: number
}

interface ModuleStatusRow {
  id: number
  order_index: number
  title: string
  kp_extraction_status: string
}

interface ModuleStatusOut {
  id: number
  orderIndex: number
  title: string
  kpExtractionStatus: 'pending' | 'processing' | 'completed' | 'failed'
  ready: boolean
}

interface BookStatusResponse {
  // Legacy fields (M1, retained for ProcessingPoller / ActionHub)
  parseStatus: string
  ocrCurrentPage: number
  ocrTotalPages: number
  parse_status: string
  kp_extraction_status: string
  ocr_current_page: number
  ocr_total_pages: number
  // New fields (M4.5 preparing page)
  bookId: number
  uploadStatus: 'pending' | 'confirmed'
  kpExtractionStatus: 'pending' | 'processing' | 'completed' | 'failed'
  modules: ModuleStatusOut[]
  progressPct: number
  firstModuleReady: boolean
  estimatedSecondsRemaining: number | null
}

function normalizeStatus(raw: string): 'pending' | 'processing' | 'completed' | 'failed' {
  if (raw === 'done') return 'completed'
  if (raw === 'error') return 'failed'
  if (raw === 'running') return 'processing'
  if (raw === 'pending' || raw === 'processing' || raw === 'completed' || raw === 'failed') {
    return raw
  }
  return 'pending'
}

export const GET = handleRoute(async (req, context) => {
  const { bookId } = await context!.params
  const id = Number(bookId)

  if (!Number.isInteger(id) || id <= 0) {
    throw new UserError('Invalid book ID', 'INVALID_ID', 400)
  }

  await requireBookOwner(req, id)

  const book = await queryOne<BookStatusRow>(
    `SELECT id, upload_status, parse_status, kp_extraction_status, ocr_current_page, ocr_total_pages
     FROM books WHERE id = $1`,
    [id]
  )

  if (!book) {
    throw new UserError('Book not found', 'NOT_FOUND', 404)
  }

  const moduleRows = await query<ModuleStatusRow>(
    `SELECT id, order_index, title, kp_extraction_status
     FROM modules WHERE book_id = $1 ORDER BY order_index`,
    [id]
  )

  const modules: ModuleStatusOut[] = moduleRows.map((m) => {
    const kpStatus = normalizeStatus(m.kp_extraction_status)
    return {
      id: m.id,
      orderIndex: m.order_index,
      title: m.title,
      kpExtractionStatus: kpStatus,
      ready: kpStatus === 'completed',
    }
  })

  const parseStatus = normalizeStatus(book.parse_status)
  const kpExtractionStatus = normalizeStatus(book.kp_extraction_status)
  const uploadStatus = (book.upload_status === 'pending' ? 'pending' : 'confirmed') as
    | 'pending'
    | 'confirmed'

  const readyCount = modules.filter((m) => m.ready).length
  const totalModules = Math.max(modules.length, 1)

  let progressPct: number
  if (uploadStatus === 'pending') {
    progressPct = 0
  } else if (parseStatus === 'pending') {
    progressPct = 5
  } else if (parseStatus === 'processing') {
    const ocrPct = book.ocr_total_pages > 0
      ? (book.ocr_current_page / book.ocr_total_pages) * 30
      : 0
    progressPct = 10 + Math.min(ocrPct, 30)
  } else if (parseStatus === 'completed' && kpExtractionStatus !== 'completed') {
    progressPct = 40 + (readyCount / totalModules) * 55
  } else if (kpExtractionStatus === 'completed') {
    progressPct = 100
  } else {
    progressPct = 0
  }

  const firstModuleReady = modules[0]?.ready ?? false

  const response: BookStatusResponse = {
    // legacy
    parseStatus,
    ocrCurrentPage: book.ocr_current_page,
    ocrTotalPages: book.ocr_total_pages,
    parse_status: book.parse_status,
    kp_extraction_status: book.kp_extraction_status,
    ocr_current_page: book.ocr_current_page,
    ocr_total_pages: book.ocr_total_pages,
    // new
    bookId: book.id,
    uploadStatus,
    kpExtractionStatus,
    modules,
    progressPct: Math.round(progressPct),
    firstModuleReady,
    estimatedSecondsRemaining: null,
  }

  return { data: response }
})
```

**Notes:**
- `query` 是多行查询 helper，确认 `@/lib/db` 导出（和 `queryOne` 同文件）
- 归一化函数支持任意历史值（pending/processing/completed/failed 是目标；done/error/running 是现有输入）
- progressPct 最终 `Math.round` 避免小数

- [ ] **Step 3: 本地 curl 测试（保留旧字段）**

```bash
curl http://localhost:3000/api/books/<existing-bookId>/status -H "Cookie: <session>"
```
Expected: JSON 包含旧字段（`parseStatus`, `ocrCurrentPage` 等）+ 新字段（`bookId`, `modules`, `progressPct` 等）。

- [ ] **Step 4: 本地测试 (pending 书)**

用 Task 3 创建一个书但不 confirm：
```bash
curl http://localhost:3000/api/books/<pending-id>/status -H "Cookie: <session>"
```
Expected: `uploadStatus: 'pending'`, `progressPct: 0`, `firstModuleReady: false`。

- [ ] **Step 5: 本地测试 (completed 书)**

选一本 M4 已完成的书：
```bash
curl http://localhost:3000/api/books/<done-id>/status -H "Cookie: <session>"
```
Expected: `uploadStatus: 'confirmed'`, `parseStatus: 'completed'`, `progressPct: 100`, `modules` 数组非空。

- [ ] **Step 6: 确认 ProcessingPoller 不坏**

在浏览器访问一本处理中的书的 reader 页（或其他用 ProcessingPoller 的页面）。
Expected: 页面正常轮询，不报错（旧字段仍可读）。

- [ ] **Step 7: Commit**

```bash
git add src/app/api/books/[bookId]/status/route.ts
git commit -m "feat(m4.5): extend status endpoint with modules + progressPct

Adds 7 new fields to GET /api/books/[bookId]/status for the preparing
page: bookId, uploadStatus, kpExtractionStatus, modules[], progressPct,
firstModuleReady, estimatedSecondsRemaining.

All legacy fields (parseStatus/ocrCurrentPage/snake_case) preserved
for ProcessingPoller and ActionHub compatibility.

Refs: spec §2.4"
```

---

## Task 7: 重写 upload page — XHR 状态机 + PDF/TXT 分流

**Files:**
- Modify: `src/app/upload/page.tsx`

**Context:** 视觉骨架不动，重写"按按钮之后"逻辑。PDF 走 presign → XHR PUT → confirm 新链路；TXT 继续走老 POST /api/books。

- [ ] **Step 1: 读当前 upload page.tsx，保留视觉骨架**

Run: Read `src/app/upload/page.tsx`
确认：form + file input + title input + submit button 区域不动；`status` state 定义需扩展。

- [ ] **Step 2: 新状态机类型定义**

替换 L13：
```ts
type UploadStatus =
  | { kind: 'idle' }
  | { kind: 'signing' }
  | { kind: 'uploading'; pct: number; loadedBytes: number; totalBytes: number }
  | { kind: 'confirming' }
  | { kind: 'redirecting' }
  | { kind: 'error'; message: string; retryTo: 'idle' | 'books' }

type UploadStatusKind = UploadStatus['kind']
```

- [ ] **Step 3: 重写 handleSubmit 函数**

替换 L48-76 整个 handleSubmit：

```ts
async function handleSubmit(e: React.FormEvent) {
  e.preventDefault()
  if (!file) { setError('请先选择一个文件'); return }
  if (!title.trim()) { setError('请输入教材标题'); return }
  setError('')

  const isPdf = file.name.toLowerCase().endsWith('.pdf')

  if (!isPdf) {
    // TXT branch: keep legacy POST /api/books flow
    setStatus({ kind: 'signing' })
    const formData = new FormData()
    formData.append('file', file)
    formData.append('title', title.trim())
    try {
      const res = await fetch('/api/books', { method: 'POST', body: formData })
      const data = await res.json()
      if (!res.ok) {
        setStatus({ kind: 'error', message: data.error ?? '上传失败，请重试', retryTo: 'idle' })
        return
      }
      setStatus({ kind: 'redirecting' })
      router.push(`/books/${data.bookId}/reader`)
    } catch {
      setStatus({ kind: 'error', message: '网络请求失败', retryTo: 'idle' })
    }
    return
  }

  // PDF branch: presign → XHR PUT → confirm
  if (file.size > 50 * 1024 * 1024) {
    setError('文件太大，请控制在 50MB 以内')
    return
  }

  try {
    // Step 1: signing
    setStatus({ kind: 'signing' })
    const presignRes = await fetch('/api/uploads/presign', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        filename: file.name,
        size: file.size,
        contentType: 'application/pdf',
      }),
    })
    const presignJson = await presignRes.json()
    if (!presignRes.ok) {
      setStatus({
        kind: 'error',
        message: presignJson.error ?? '系统繁忙，请稍后重试',
        retryTo: 'idle',
      })
      return
    }
    const { bookId, uploadUrl } = presignJson.data as { bookId: number; uploadUrl: string }

    // Step 2: XHR PUT with progress
    await new Promise<void>((resolve, reject) => {
      const xhr = new XMLHttpRequest()
      xhr.open('PUT', uploadUrl)
      xhr.setRequestHeader('Content-Type', 'application/pdf')
      xhr.upload.onprogress = (e) => {
        if (!e.lengthComputable) return
        setStatus({
          kind: 'uploading',
          pct: Math.round((e.loaded / e.total) * 100),
          loadedBytes: e.loaded,
          totalBytes: e.total,
        })
      }
      xhr.onload = () => {
        if (xhr.status >= 200 && xhr.status < 300) resolve()
        else if (xhr.status === 403) reject(new Error('网络太慢，请换个网络重试'))
        else reject(new Error(`上传失败 (HTTP ${xhr.status})`))
      }
      xhr.onerror = () => reject(new Error('网络中断，请点击重试'))
      xhr.ontimeout = () => reject(new Error('网络中断，请点击重试'))
      xhr.send(file)
    })

    // Step 3: confirming
    setStatus({ kind: 'confirming' })
    const confirmRes = await fetch('/api/books/confirm', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ bookId, title: title.trim() }),
    })
    const confirmJson = await confirmRes.json()
    if (confirmRes.status === 409 && confirmJson.code === 'PROCESSING_FAILED') {
      setStatus({
        kind: 'error',
        message: confirmJson.error ?? '上一次处理失败，请删除书重新上传',
        retryTo: 'books',
      })
      return
    }
    if (!confirmRes.ok) {
      setStatus({
        kind: 'error',
        message: confirmJson.error ?? '整理失败，请重试',
        retryTo: 'idle',
      })
      return
    }

    // Step 4: redirect to preparing page
    setStatus({ kind: 'redirecting' })
    router.push(`/books/${bookId}/preparing`)
  } catch (err) {
    setStatus({
      kind: 'error',
      message: err instanceof Error ? err.message : '上传失败',
      retryTo: 'idle',
    })
  }
}

function handleRetry() {
  const currentStatus = status
  if (currentStatus.kind === 'error' && currentStatus.retryTo === 'books') {
    router.push('/books')
    return
  }
  setStatus({ kind: 'idle' })
  setError('')
}
```

- [ ] **Step 4: 改 state 初始值 + 替换 status 判断**

替换 L22：
```ts
const [status, setStatus] = useState<UploadStatus>({ kind: 'idle' })
```

替换 L102 的 `status !== 'idle'` 判断和 L105-106 的 label：

```tsx
{status.kind !== 'idle' ? (
  <div className="py-10 text-center space-y-6">
    {status.kind === 'signing' && (
      <LoadingState label="正在准备上传..." />
    )}
    {status.kind === 'uploading' && (
      <div className="space-y-4">
        <LoadingState label={`正在上传 ${status.pct}%`} />
        <div className="w-full bg-surface-container-low rounded-full h-2 overflow-hidden">
          <div
            className="bg-primary h-2 transition-all duration-200"
            style={{ width: `${status.pct}%` }}
          />
        </div>
        <p className="text-xs text-on-surface-variant">
          {(status.loadedBytes / 1024 / 1024).toFixed(1)} / {(status.totalBytes / 1024 / 1024).toFixed(1)} MB
        </p>
      </div>
    )}
    {status.kind === 'confirming' && (
      <LoadingState label="整理中，马上好..." />
    )}
    {status.kind === 'redirecting' && (
      <LoadingState label="准备就绪，正在跳转..." />
    )}
    {status.kind === 'error' && (
      <div className="space-y-4">
        <div className="bg-error/10 text-error p-4 rounded-xl text-sm font-bold flex items-center justify-center gap-3">
          <span className="material-symbols-outlined text-lg">error</span>
          {status.message}
        </div>
        <AmberButton onClick={handleRetry} fullWidth>
          {status.retryTo === 'books' ? '前往书架' : '重试'}
        </AmberButton>
      </div>
    )}
  </div>
) : (
  // ... existing form markup unchanged
)}
```

- [ ] **Step 5: 本地测试 TXT 上传（回归）**

Run: `npm run dev` → 浏览器选一个 .txt 文件 + 输入标题 → 点 "开始智能学习"。
Expected: 跳 `/books/<id>/reader`，reader 页显示 TXT 内容。

- [ ] **Step 6: 本地测试小 PDF 上传（<4.5MB）**

浏览器选小 PDF → 观察：
- `signing` 状态（<1s）
- `uploading` 进度条 0-100%
- `confirming` 状态（~1s）
- 跳 `/books/<id>/preparing`（Task 8 前先跳到 404，下一个任务做 preparing 页）

Expected: 4 个状态依次出现，进度条流畅，无卡顿。

- [ ] **Step 7: 本地测试超限 PDF（>50MB → 错误）**

浏览器选 60MB PDF。
Expected: 点按钮后立刻显示 "文件太大，请控制在 50MB 以内"（客户端拦）。

- [ ] **Step 8: 本地测试 PDF presign 失败（模拟）**

临时把 `/api/uploads/presign` 的 size cap 改成 1024（极小）让任何真 PDF 都被拒。
刷新页面 → 选 PDF → 提交。
Expected: error 状态 + 重试按钮，点击后回 idle 保留文件和标题。

恢复代码：把 cap 改回 50MB。

- [ ] **Step 9: Commit**

```bash
git add src/app/upload/page.tsx
git commit -m "feat(m4.5): rewrite upload page state machine + PDF/TXT split

PDF branch: presign → XHR PUT to R2 with upload.onprogress → confirm →
redirect to /preparing. Shows real upload percentage + byte counts.
TXT branch retained: legacy POST /api/books → /reader.

6-state machine: idle / signing / uploading(%) / confirming /
redirecting / error. Error states retry to idle (transient) or /books
(after confirmed-but-failed 409).

Visual skeleton unchanged (sidebar, form, components).

Refs: spec §3.1"
```

---

## Task 8: 新建 /preparing 页 + loading.tsx

**Files:**
- Create: `src/app/books/[bookId]/preparing/page.tsx`
- Create: `src/app/books/[bookId]/preparing/loading.tsx`

**Context:** 用户点 confirm → router.push `/preparing` 瞬间，Next.js 先渲染 loading.tsx（0ms 骨架屏），然后 page.tsx 挂载启动 2s polling。第一模块 kp_extraction_status='completed' 时解锁按钮；用户手动点跳 /reader。

- [ ] **Step 1: 创建 loading.tsx（Next.js 自动显示）**

Create: `src/app/books/[bookId]/preparing/loading.tsx`

```tsx
export default function PreparingLoading() {
  return (
    <div className="min-h-screen bg-surface-container-low flex items-center justify-center p-10">
      <div className="max-w-2xl w-full space-y-8">
        <div className="text-center space-y-3">
          <div className="h-8 bg-surface-container animate-pulse rounded-lg w-3/4 mx-auto" />
          <div className="h-4 bg-surface-container animate-pulse rounded w-1/2 mx-auto" />
        </div>
        <div className="h-2 bg-surface-container animate-pulse rounded-full" />
        <div className="grid grid-cols-1 gap-3">
          {[1, 2, 3, 4, 5].map((i) => (
            <div key={i} className="h-16 bg-surface-container animate-pulse rounded-2xl" />
          ))}
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 创建 page.tsx**

Create: `src/app/books/[bookId]/preparing/page.tsx`

```tsx
'use client'

import { useState, useEffect, useRef } from 'react'
import { useRouter } from 'next/navigation'
import { use } from 'react'
import AppSidebar from '@/components/ui/AppSidebar'
import ContentCard from '@/components/ui/ContentCard'
import AmberButton from '@/components/ui/AmberButton'
import DecorativeBlur from '@/components/ui/DecorativeBlur'

interface ModuleStatus {
  id: number
  orderIndex: number
  title: string
  kpExtractionStatus: 'pending' | 'processing' | 'completed' | 'failed'
  ready: boolean
}

interface BookStatus {
  bookId: number
  uploadStatus: 'pending' | 'confirmed'
  parseStatus: 'pending' | 'processing' | 'completed' | 'failed'
  kpExtractionStatus: 'pending' | 'processing' | 'completed' | 'failed'
  modules: ModuleStatus[]
  progressPct: number
  firstModuleReady: boolean
}

interface BookMeta { title: string }

function statusText(s: BookStatus | null): string {
  if (!s) return '正在为你准备这本书...'
  if (s.uploadStatus === 'pending') return '上传中...'
  if (s.parseStatus === 'pending') return '开始处理...'
  if (s.parseStatus === 'processing') return '正在识别页面内容...'
  if (s.parseStatus === 'completed' && s.kpExtractionStatus !== 'completed') {
    return '正在提取知识点...'
  }
  if (s.kpExtractionStatus === 'completed') return '全部准备完成！'
  return '正在为你准备这本书...'
}

export default function PreparingPage({ params }: { params: Promise<{ bookId: string }> }) {
  const { bookId } = use(params)
  const bookIdNum = Number(bookId)
  const router = useRouter()

  const [bookMeta, setBookMeta] = useState<BookMeta | null>(null)
  const [status, setStatus] = useState<BookStatus | null>(null)
  const [error, setError] = useState<string | null>(null)
  const [userName, setUserName] = useState('用户')
  const pollRef = useRef<ReturnType<typeof setInterval> | null>(null)

  // Fetch user display name
  useEffect(() => {
    fetch('/api/auth/me')
      .then(r => r.json())
      .then(d => { if (d.success) setUserName(d.data.display_name || d.data.email) })
      .catch(() => {})
  }, [])

  // Fetch book title once
  useEffect(() => {
    fetch(`/api/books/${bookIdNum}`)
      .then(r => r.json())
      .then(d => {
        if (d.data?.title) setBookMeta({ title: d.data.title })
      })
      .catch(() => {})
  }, [bookIdNum])

  // Polling
  useEffect(() => {
    async function poll() {
      try {
        const res = await fetch(`/api/books/${bookIdNum}/status`)
        if (!res.ok) {
          if (res.status === 404) {
            setError('书不存在或无权访问')
            if (pollRef.current) clearInterval(pollRef.current)
          }
          return
        }
        const json = await res.json()
        const s = json.data as BookStatus
        setStatus(s)
        if (s.parseStatus === 'failed' || s.kpExtractionStatus === 'failed') {
          setError('处理出错，请删除书重新上传')
          if (pollRef.current) clearInterval(pollRef.current)
        }
      } catch {
        // transient network error — keep polling
      }
    }
    poll()
    pollRef.current = setInterval(poll, 2000)
    return () => {
      if (pollRef.current) clearInterval(pollRef.current)
    }
  }, [bookIdNum])

  const firstReady = status?.firstModuleReady ?? false
  const allReady = status?.kpExtractionStatus === 'completed'

  const buttonLabel = allReady
    ? '开始阅读 →'
    : firstReady
      ? '开始阅读第一模块 →'
      : '准备中...'

  const navItems = [
    { icon: 'home', label: '首页中心', href: '/' },
    { icon: 'cloud_upload', label: '上传教材', href: '/upload' },
    { icon: 'analytics', label: '系统日志', href: '/logs' },
  ]

  return (
    <div className="min-h-screen bg-surface-container-low">
      <AppSidebar userName={userName} navItems={navItems} />
      <main className="ml-72 p-10 relative min-h-screen flex items-center justify-center">
        <DecorativeBlur position="top-right" />
        <DecorativeBlur position="bottom-left" color="secondary" />

        <div className="max-w-2xl w-full relative z-10 space-y-8">
          <header className="text-center space-y-2">
            <h1 className="text-3xl font-black text-on-surface font-headline tracking-tight">
              {bookMeta?.title ?? '正在载入...'}
            </h1>
            <p className="text-on-surface-variant font-medium">正在为你准备这本书</p>
          </header>

          {error ? (
            <ContentCard className="p-8 text-center space-y-4">
              <div className="w-16 h-16 bg-error/10 text-error rounded-2xl flex items-center justify-center mx-auto">
                <span className="material-symbols-outlined text-3xl">error</span>
              </div>
              <p className="text-sm font-bold text-on-surface">{error}</p>
              <AmberButton onClick={() => router.push('/books')} fullWidth>
                前往书架
              </AmberButton>
            </ContentCard>
          ) : (
            <>
              {/* Progress bar */}
              <div className="space-y-2">
                <div className="w-full bg-surface-container rounded-full h-2 overflow-hidden">
                  <div
                    className="bg-primary h-2 transition-all duration-500 ease-out"
                    style={{ width: `${status?.progressPct ?? 0}%` }}
                  />
                </div>
                <p className="text-xs text-on-surface-variant text-center font-medium">
                  {statusText(status)} · {status?.progressPct ?? 0}%
                </p>
              </div>

              {/* Module skeleton grid */}
              <div className="grid grid-cols-1 gap-3">
                {(status?.modules.length ? status.modules : [1, 2, 3, 4, 5].map(i => null)).map((m, idx) => {
                  const isReal = typeof m === 'object' && m !== null
                  const ready = isReal && m.ready
                  return (
                    <div
                      key={isReal ? m.id : `skel-${idx}`}
                      className={
                        ready
                          ? 'bg-surface rounded-2xl p-4 border border-outline-variant/20 transition-all duration-300'
                          : 'h-16 bg-surface-container animate-pulse rounded-2xl'
                      }
                    >
                      {ready && (
                        <div className="flex items-center gap-3">
                          <div className="w-10 h-10 bg-primary/10 text-primary rounded-xl flex items-center justify-center font-bold">
                            {m.orderIndex + 1}
                          </div>
                          <span className="text-sm font-bold text-on-surface flex-1">{m.title}</span>
                          <span className="material-symbols-outlined text-primary">check_circle</span>
                        </div>
                      )}
                    </div>
                  )
                })}
              </div>

              {/* Start button */}
              <AmberButton
                fullWidth
                size="lg"
                disabled={!firstReady}
                onClick={() => router.replace(`/books/${bookIdNum}/reader`)}
              >
                {buttonLabel}
              </AmberButton>
            </>
          )}
        </div>
      </main>
    </div>
  )
}
```

**Notes:**
- `router.replace` 而非 `push`——避免返回键回到 preparing 页
- 骨架屏初始 5 个占位，拿到真 modules 后用真实数据（modules.length > 0 时）
- 错误态直接显示 "前往书架" 按钮（对应 §5.2 preparing 页错误场景）
- `GET /api/books/${bookIdNum}` 假设存在；若不存在跳过书名 fetch（degrade 为 "正在载入..."）——验证现有 API 是否返此结构

- [ ] **Step 3: 验证 GET /api/books/[bookId] 是否返 `{ data: { title } }`**

Run: Check if file exists — `ls src/app/api/books/[bookId]/route.ts`

**决策树**（不要扩展 status 端点或在本任务新建路由）：
- (a) 文件存在 + 返回 `{ data: { title } }` → Step 2 的 bookMeta fetch 保持原样
- (b) 文件存在但返回结构不同 → 按实际字段名微调（e.g. `data.book.title`）
- (c) 文件不存在 → **直接删除 Step 2 中的 bookMeta useEffect + title fetch**，书名固定显示 "正在准备的书"。**不在本任务新建 `/api/books/[bookId]` 路由**（超出 §4 preparing 页职责）

- [ ] **Step 4: 本地测试——完整链路**

Run: `npm run dev`
浏览器：登录 → 上传小 PDF → upload 页跳 `/books/<id>/preparing` → 观察：
- loading.tsx 初始 0ms 骨架屏
- page.tsx 挂载后 2s 内首次 poll 成功
- 进度条 0 → 5 → 10-40 → 40-95 → 100 渐进
- 第一模块 kp ready 时按钮从灰变 amber + 文案"开始阅读第一模块 →"
- 点按钮跳 `/books/<id>/reader`

- [ ] **Step 5: 本地测试——失败态**

模拟 classify-pdf 失败：临时把 OCR_SERVER_URL 指向不存在的 URL `http://localhost:9999`。
重启 dev → 重做一次上传 → 准备页应该显示 "处理出错，请删除书重新上传" + "前往书架" 按钮。
恢复 OCR_SERVER_URL。

- [ ] **Step 6: 本地测试——直接访问不存在的 bookId**

浏览器访问 `http://localhost:3000/books/99999/preparing`。
Expected: 显示 "书不存在或无权访问" + "前往书架" 按钮。

- [ ] **Step 7: Commit**

```bash
git add src/app/books/[bookId]/preparing/page.tsx src/app/books/[bookId]/preparing/loading.tsx
git commit -m "feat(m4.5): add /preparing page with polling + skeleton

Next.js App Router loading.tsx shows 0ms skeleton on navigation.
page.tsx polls /api/books/[bookId]/status every 2s until first module
kpExtractionStatus === 'completed', then unlocks the 'Start reading
first module →' button. User manually clicks to jump to /reader via
router.replace (no back-button trap).

Progress bar: 0 → 5 → 10-40 → 40-95 → 100 staged per spec §2.4
algorithm. Harrison CHI 2010 'backwards decelerating' psychology:
95→100 jump feels like finish, better than linear approach.

Error state (parseStatus=failed or 404) shows 'delete and retry' CTA
pointing to /books.

Refs: spec §3.2, §3.3"
```

---

## Task 9: 基础设施手动配置（用户执行）

**Files:**
- 无代码改动，仅云控制台配置

**Context:** R2 CORS + Vercel Fluid Compute 是一次性手动操作。Claude 不做，指导用户做。

- [ ] **Step 1: 配置 R2 CORS Policy**

用户操作：
1. 登录 Cloudflare Dashboard
2. 进入 R2 → 选中生产 bucket（教材 PDF 存储桶）
3. Settings → CORS Policy
4. 粘贴 JSON（替换 `<production-domain>` 为实际生产域名，如 `ai-textbook.vercel.app`）：

```json
[
  {
    "AllowedOrigins": [
      "https://<production-domain>",
      "http://localhost:3000"
    ],
    "AllowedMethods": ["PUT", "HEAD"],
    "AllowedHeaders": ["Content-Type"],
    "ExposeHeaders": ["ETag"],
    "MaxAgeSeconds": 3600
  }
]
```

5. 保存，等待 2-3 分钟生效

- [ ] **Step 2: 验证 R2 CORS（用 curl OPTIONS 预检）**

```bash
curl -X OPTIONS "https://<account>.r2.cloudflarestorage.com/<bucket>/books/99/original.pdf" \
  -H "Origin: https://<production-domain>" \
  -H "Access-Control-Request-Method: PUT" \
  -H "Access-Control-Request-Headers: Content-Type" \
  -I
```
Expected: `Access-Control-Allow-Origin: <production-domain>`，`Access-Control-Allow-Methods: PUT, HEAD`。

- [ ] **Step 3: 验证 Vercel Fluid Compute 已启用**

用户操作：
1. 登录 Vercel Dashboard
2. 进入 ai-textbook-teacher 项目 → Settings → Functions
3. 确认 "Fluid Compute" toggle 为 ON
4. 若未开启 → 手动启用，触发一次 redeploy

- [ ] **Step 4: 验证 Fluid Compute 生效**

Vercel Dashboard → Deployments → 最新 deploy → Build Logs，搜索 `Fluid`。
Expected: 出现 `Runtime: Fluid` 或类似行。

- [ ] **Step 5: 登记操作完成（无 commit）**

Claude 在 journal 里记录：`docs/journal/2026-04-21-m4.5-infra-setup.md`
```markdown
# M4.5 基础设施手动配置完成

**日期**: 2026-04-21
**操作人**: 用户

- ✅ R2 CORS Policy 已配（允许 PUT/HEAD 从生产域 + localhost）
- ✅ Vercel Fluid Compute toggle 已确认 ON

**验证命令留底**：（详见 plan Step 2/4）
```

---

## Task 10: 14.2MB 端到端压测（生产环境）

**Files:**
- 无代码改动，仅测试执行 + 结果登记

**Context:** Spec §6 明确：不在本地 Docker，不用 mock。在生产 Vercel Hobby + Cloud Run + R2 真跑 14.2MB 369 页中文书。

- [ ] **Step 1: 前置确认**

- [ ] Task 1-8 全部 commit 且 master push 到 Vercel 部署
- [ ] Task 9 R2 CORS + Fluid Compute 完成
- [ ] Sentry Dashboard 打开准备监控错误
- [ ] Vercel logs 打开准备监控函数执行
- [ ] Neon Dashboard 打开准备监控 DB 状态

- [ ] **Step 2: 执行上传**

用户：
1. 打开生产域名，登录
2. 点 "上传教材"
3. 选择 14.2MB 目标 PDF，填标题
4. 点 "开始智能学习"
5. **同时计时**：点击 → 看到 "正在准备" 文案的时间

- [ ] **Step 3: 验证感知 5s**

Expected: 从点击到跳 `/preparing` ≤ 5 秒（包含 presign + XHR PUT 开始 + confirm 返回）。
若 > 5s：检查 presign 响应时间 + XHR PUT 开始延迟。

- [ ] **Step 4: 验证感知 30s**

Expected: 进 `/preparing` 后 30 秒内第一模块解锁按钮亮起。
若 > 30s：看 Vercel logs 中 classify-pdf + 第一模块 KP extraction 时间戳。

- [ ] **Step 5: 验证端到端完成**

Expected: 全部模块就绪（progressPct=100）< 5 分钟。
若超 5 分钟或卡住：
- `parseStatus='failed'` → 检查 Sentry 错误 → 走 §6.4 fallback plan
- `parseStatus='processing'` 死卡 → Fluid 函数可能被杀，走 §6.4 fallback

- [ ] **Step 6: 验证功能完整**

- [ ] 点 "开始阅读第一模块 →" 进 reader，PDF + 模块列表正常
- [ ] 第一模块 Q&A 可触发，AI 响应正常
- [ ] 教学模式切换正常（M4 paywall UI 正确）

- [ ] **Step 7: 回归 checklist**

- [ ] TXT 小文件上传 work
- [ ] <4.5MB PDF 走新 presign → confirm OK
- [ ] M3 复习流程完整（`GET /api/books` 返回列表正常，不含孤儿）
- [ ] M4 teaching 流程完整
- [ ] `GET /api/books/[id]/pdf` 302 正常

- [ ] **Step 8: 若撞 300s Fluid 顶，走 fallback plan**

触发条件：`parseStatus='processing'` 超过 5 分钟不动 AND Vercel 函数日志显示实例被 terminated。

Fallback：按 §6.4 方案，改 `classify-pdf` / `extract-text` 为 Cloud Run 自身 fire-and-forget + callback 路由：
- 新建 `src/app/api/internal/extract-done/route.ts`（仿 `src/app/api/ocr/callback/route.ts`）
- 修 `src/lib/upload-flow.ts` 把 classify+extract 改为 fire-and-forget，返回后 Cloud Run 自己完成后 POST callback
- 工作量 1-2 天，独立 commit

- [ ] **Step 9: 登记压测结果**

Claude 写 `docs/journal/2026-04-21-m4.5-stress-test-14mb.md`：
```markdown
# M4.5 14.2MB 压测结果

**日期**: 2026-04-21
**测试文件**: <书名> / 14.2MB / 369 页
**环境**: 生产 Vercel Hobby + Fluid ON / Cloud Run / R2

**验收**:
- [x/❌] 感知 5s 进准备页: 实测 X.X 秒
- [x/❌] 感知 30s 第一模块就绪: 实测 XX 秒
- [x/❌] 端到端完成 <5 分钟: 实测 XXX 秒
- [x/❌] Q&A 触发正常
- [x/❌] 教学模式切换正常

**回归**:
- [x/❌] TXT 上传
- [x/❌] 小 PDF 上传
- [x/❌] M3 复习
- [x/❌] M4 teaching
- [x/❌] PDF GET 302

**发现的问题**: <若有>

**是否触发 fallback**: 否 / 是（原因 + 处理）
```

---

## Task 11: 文档闭环 + 停车场归档

**Files:**
- Modify: `docs/architecture.md`（摘要卡 + §X 上传流程）
- Modify: `docs/changelog.md`
- Modify: `docs/journal/INDEX.md`（移除 T2 pointer）
- Modify: `docs/project_status.md`（M4.5 完成标识）
- Modify: `docs/superpowers/INDEX.md`（spec 移到 resolved 段）

**Context:** 所有 docs 同步更新，避免 "architecture 鲜度失守" 风险（CLAUDE.md 明文规则）。

- [ ] **Step 1: 更新 architecture.md §0 摘要卡**

在 `books` 表字段描述下追加 `upload_status` + `file_size` 两列语义。
在 "API 接口契约" 段追加 3 个端点：
- `POST /api/uploads/presign` — 签发 R2 PUT URL，`requireUser`
- `POST /api/books/confirm` — 确认上传完成 + fire-and-forget 启 classify
- `GET /api/books/[bookId]/status` — 扩展后字段（旧字段保留）

更新上传流程图（文字描述）：
```
浏览器 → POST /api/uploads/presign → (拿 bookId + uploadUrl)
      → XHR PUT R2
      → POST /api/books/confirm → (HEAD R2 + UPDATE + fire-and-forget classify)
      → router.push /preparing → polling /status → 按钮 → /reader
```

- [ ] **Step 2: 更新 changelog.md**

追加：
```markdown
## 2026-04-21 · M4.5 PDF 上传重构

- feat: Presigned URL 直传 R2，支持 >4.5MB PDF（之前 Vercel Hobby body 限制阻塞 14.2MB 真书）
- feat: /api/uploads/presign 端点 + /api/books/confirm 端点 + /books/[id]/preparing 准备页
- feat: upload 页 XHR onprogress 进度条 + PDF/TXT 分流
- feat: status 端点扩展 modules/progressPct/firstModuleReady 字段
- perf: confirm fire-and-forget 让用户感知 5s 内跳准备页
- db: books 加 upload_status + file_size 两列（现有 books 默认 'confirmed' 不影响）
- infra: R2 CORS (PUT/HEAD) + Vercel Fluid Compute 确认

Refs: spec `2026-04-21-pdf-upload-refactor-design.md` · plan `2026-04-21-m4.5-pdf-upload-refactor.md`
```

- [ ] **Step 3: 移除停车场 T2 pointer**

Edit `docs/journal/INDEX.md`：找 `2026-04-19-pdf-upload-size-limit.md` 条目（在 T2 段），删除。
Edit `docs/journal/INDEX-resolved.md`（归档段）：追加该条目 + 解决说明 "M4.5 完成"。

- [ ] **Step 4: 更新 project_status.md**

把"进行中：M4.5" 改成 "已完成"，追加到 §1.已完成段：
```markdown
- **M4.5 PDF 上传重构** ✅ 完成（2026-04-21）：Presigned URL 直传 R2 + fire-and-forget + 准备页 polling。14.2MB 真书端到端通过。→ [spec](...) · [plan](...)
```
"进行中"改回 "无"；"下一步" 改回用户决策方向菜单（剔除"处理停车场 T2"选项，因已解决）。

- [ ] **Step 5: 更新 superpowers/INDEX.md**

把 `2026-04-21-pdf-upload-refactor-design.md` 从 "进行中（in_progress）" 段移到 "已完成（resolved）" 段。
追加 plan 到 Plans 段 "已完成"：
```markdown
- [2026-04-21] M4.5 PDF 上传重构 `[M4.5, PDF-upload, presigned-URL, R2-direct-upload, preparing-page, skeleton-screen, fire-and-forget]` → [plan](plans/2026-04-21-m4.5-pdf-upload-refactor.md)
```

- [ ] **Step 6: 若 CLAUDE.md 含 "4.5MB" 说明行，删除**

Run: `grep -n "4.5MB" CLAUDE.md`
若命中，删除或改成 "已在 M4.5 解决（2026-04-21）"。

- [ ] **Step 7: 删除 memory MEMORY.md 的 M4.5 WIP pointer**

用户主目录 memory：`C:\Users\Administrator\.claude\projects\d------Users-Sean-ai-textbook-teacher\memory\`
- 删除 `project_m4-5-brainstorm-wip.md` 文件
- 从 `MEMORY.md` 索引删除 "M4.5 Brainstorm WIP" 那一行
- 在 `docs/memory-audit-log.md` 追加 delete 条目

- [ ] **Step 8: Commit 所有 doc 改动**

```bash
git add docs/architecture.md docs/changelog.md docs/journal/INDEX.md docs/journal/INDEX-resolved.md \
        docs/project_status.md docs/superpowers/INDEX.md docs/memory-audit-log.md CLAUDE.md
git commit -m "docs(m4.5): closeout — architecture/changelog/status/INDEX sync

- architecture.md: books schema + new endpoints + updated upload flow
- changelog.md: M4.5 2026-04-21 entry
- journal INDEX: remove T2 parking lot pointer (resolved)
- project_status.md: M4.5 done, next-step menu refreshed
- superpowers/INDEX: spec → resolved, plan added
- memory: WIP pointer deleted + audit log updated

M4.5 complete end-to-end."
```

---

## 收尾 Checklist

M4.5 完整交付的确认清单：

- [ ] Task 1 ✅ DB 2 列迁移完成，现有数据 OK
- [ ] Task 2 ✅ r2-client.ts 新增函数 + 测试 PASS
- [ ] Task 3 ✅ /presign 端点 4 个 curl 场景全通过
- [ ] Task 4 ✅ /confirm 端点 + upload-flow.ts + 幂等 + fire-and-forget
- [ ] Task 5 ✅ /api/books PDF 分支删除 + TXT 保留
- [ ] Task 6 ✅ status 端点扩展 + 旧字段保留
- [ ] Task 7 ✅ upload 页状态机 + XHR + PDF/TXT 分流
- [ ] Task 8 ✅ /preparing 页 + loading + 骨架屏 + polling
- [ ] Task 9 ✅ R2 CORS + Fluid 配置验证
- [ ] Task 10 ✅ 14.2MB 真书压测通过（感知 5s + 感知 30s + 端到端 <5min）
- [ ] Task 11 ✅ 所有 docs 同步 + 停车场下架 + memory 清理

---

## 风险预案

| 风险 | 触发条件 | 应对 |
|---|---|---|
| Fluid 300s 撞顶 | parseStatus='processing' >5min | §6.4 fallback：classify/extract 改 Cloud Run callback 模式 |
| R2 CORS 配错 | XHR PUT 返 403 + 浏览器 CORS 报错 | Task 9 Step 2 curl OPTIONS 验证；检查 AllowedOrigins 协议 (https vs http) |
| confirm 内 fire-and-forget 未跑完就死 | 生产 vercel logs 看到函数 terminated 但无 classify log | Fluid ON 确认 + 考虑 @vercel/functions waitUntil 包装 fire-and-forget |
| 幂等短路漏触发 classify | 用户双击确认按钮 | UPDATE 的 WHERE 子句已加 `upload_status = 'pending'`，第二次 UPDATE 0 rows 不重入 |
| status route 旧消费方坏 | ProcessingPoller 在 reader 页报错 | Task 6 Step 6 验证；旧字段完整保留 |

---

## 调研溯源

- `docs/research/2026-04-20-pdf-upload-speed-options.md` — 29S+11A+2B 源，4 维度 sub-agent 调研
  - D1 现架构性能基线（Vercel Fluid 300s）
  - D2 OCR 替代方案（全部不采用）
  - D3 UX 层（Nielsen / Harrison CHI 2010 / Figma 渐进）
  - D4 Vercel 超时（4.5MB body 硬限 + 直传对象存储推荐）

## Spec 决策 trail

- `docs/superpowers/specs/2026-04-21-pdf-upload-refactor-brainstorm-state.md` — 8 决策拍板过程 + 拒绝替代方案

## Review 痕迹

Spec 经 2 轮 subagent 深度 review：
- Round 1: 3 Critical + 4 Important + 若干 Minor 发现（file_path 列实际未用 / objectKey 格式矛盾 / status route 已存在 / 幂等短路漏失败态 / kp_extraction_status 枚举不一致 / confirm 同步 300s 撞顶 / list query 破坏书级页面）
- Round 2: 全部 Critical + Important 已修，Verdict: APPROVED WITH MINOR
