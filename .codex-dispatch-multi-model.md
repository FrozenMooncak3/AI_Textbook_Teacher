[DISPATCH TO: Codex] # CCB_CONFIRMED

## Context
T0 priority: Replace hardcoded Anthropic SDK with Vercel AI SDK so a single env var switches all AI calls to any provider/model. This enables free testing with Gemini Flash instead of burning Claude API credits.

Plan: `docs/superpowers/plans/2026-03-29-multi-model-abstraction.md`
Spec: `docs/superpowers/specs/2026-03-29-multi-model-abstraction-design.md`

M2 (Coach AI) is complete. This is a cross-cutting refactor affecting all AI call sites.

## Task
Execute Tasks 0-6 from the plan file above. The plan contains exact code, file paths, and step-by-step instructions. Summary:

- **T0**: `npm install ai @ai-sdk/anthropic @ai-sdk/google @ai-sdk/openai`
- **T1**: Create `src/lib/ai.ts` — provider registry with Anthropic/Google/OpenAI, proxy support, `getModel()` export. Full code is in the plan.
- **T2**: Migrate 9 Pattern A files (single-turn text prompt) — mechanical import + call replacement. File list in plan.
- **T3**: Migrate `src/lib/services/kp-extraction-service.ts` — same Pattern A but has a `callClaude()` helper wrapper. Refactor/inline this wrapper to use `generateText`. Remove the `type !== 'text'` check since `generateText` always returns a string.
- **T4**: Migrate `src/app/api/conversations/[conversationId]/messages/route.ts` — Pattern B (multi-turn + system prompt). Use `messages` + `system` params in `generateText`.
- **T5**: Migrate `src/app/api/books/[bookId]/screenshot-ask/route.ts` — Pattern C (vision + system prompt). Convert Anthropic image block format to Vercel AI SDK format (`image: Buffer.from(base64Data, 'base64'), mimeType: 'image/png'`).
- **T6**: Delete `src/lib/claude.ts`. Grep to confirm no remaining imports. Create `.env.example` with `AI_MODEL` + provider key vars.

Important notes:
- `kp-extraction-service.ts` uses relative import `'../claude'` not `'@/lib/claude'`
- Default `AI_MODEL` should be `anthropic:claude-sonnet-4-6` to preserve current behavior
- Keep all JSON parsing logic (`/\{[\s\S]*\}/` regex) in each route — only the AI call changes
- Timeout: use `abortSignal: AbortSignal.timeout(300_000)` in each `generateText` call
- Do NOT trigger any Claude API calls for testing. Build verification only (`npx next build`).

## Files
- **Create**: `src/lib/ai.ts`, `.env.example`
- **Modify**: 12 files (see plan Tasks 2-5 for complete list)
- **Delete**: `src/lib/claude.ts`
- **Reference**: `docs/superpowers/plans/2026-03-29-multi-model-abstraction.md` (THE PRIMARY REFERENCE — read this first, it has all code)

## Acceptance Criteria
- [ ] `npm install` adds 4 new packages (ai, @ai-sdk/anthropic, @ai-sdk/google, @ai-sdk/openai)
- [ ] `src/lib/ai.ts` exists with provider registry, getModel(), proxy support
- [ ] All 12 call sites migrated to `generateText` from 'ai'
- [ ] `grep -r "from '@/lib/claude'" src/` returns zero results
- [ ] `grep -r "from '../claude'" src/` returns zero results
- [ ] `src/lib/claude.ts` is deleted
- [ ] `.env.example` exists with AI_MODEL + provider key documentation
- [ ] `npx next build` passes with zero errors
- [ ] Commit each task separately, push to master after each

## Tier
**Standard** (gpt-5.4-mini, high)

## Suggested Skills
`coding-standards`, `executing-plans`

## Post-Completion
Claude will review all changes via requesting-code-review, then update CLAUDE.md/AGENTS.md and project docs.
