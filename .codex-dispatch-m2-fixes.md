Code review found 2 issues to fix. Read AGENTS.md for completion reporting instructions — after finishing, send a report to Claude pane (pane 0) via wezterm.

Fix 1 (Critical C2): In src/app/api/modules/[moduleId]/generate-questions/route.ts, add question_type validation before DB insert. The qa_questions table has a CHECK constraint: question_type IN ('worked_example','scaffolded_mc','short_answer','comparison'). If Claude returns an invalid type string, the INSERT fails and crashes the whole transaction. Add before the transaction block:

const VALID_TYPES = new Set(['worked_example', 'scaffolded_mc', 'short_answer', 'comparison'])

Then inside the forEach loop, replace direct usage of question.type with:

const questionType = VALID_TYPES.has(question.type) ? question.type : 'short_answer'

Fix 2 (Important I1): In src/app/api/modules/[moduleId]/status/route.ts, add state transition validation. Before the UPDATE statement, read the current learning_status from DB. Then check against a valid transitions map:

const VALID_TRANSITIONS: Record<string, string[]> = {
  'unstarted': ['reading'],
  'reading': ['qa'],
  'qa': ['notes_generated'],
  'notes_generated': ['testing', 'completed'],
  'testing': ['completed'],
  'completed': [],
}

If the requested new status is not in VALID_TRANSITIONS[currentStatus], return 409 with error message like "Invalid status transition from X to Y".

Commit each fix separately, push to master, run npx next build before each commit. Then send completion report per AGENTS.md.
